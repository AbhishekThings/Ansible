---
- hosts: dev
  user: ansible
  become: yes
  connection: ssh

  tasks:
    - name: installing multiple tools at a time
      action: yum name= '{{item}}' state=present

      with_items:
        - git
        - maven
        - tree
        - httpd

